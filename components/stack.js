import React from 'react'import cn from 'classnames'import styles from './stack.module.css'function Stack({ gap = 10, column = false, flex = false, children }) {  return (    <div      className={cn([        styles.stack,        column && styles.column,        flex && styles.flexim      ])}      style={{ '--gap': `${gap}px` }}    >      {children}    </div>  )}export default Stack